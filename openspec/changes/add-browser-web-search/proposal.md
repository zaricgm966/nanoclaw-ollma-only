# 提案：增加基于浏览器的联网搜索

## 背景

当前 NanoClaw 虽然已经有最小可用的联网工具入口，但实现仍偏向轻量抓取，面对现代网页、跳转页和动态页面时稳定性不足。

## 目标

- 为 Ollama-only agent 提供更稳定的联网搜索与网页抓取能力
- 使用真实浏览器抓取网页内容，而不是只依赖 `curl` + HTML 正则
- 在明显需要联网的问题上提供自动兜底，减少模型忘记调用工具时的拒答

## 非目标

- 不在这一轮增加完整浏览器自动化工作流
- 不在这一轮增加通用登录态管理
- 不在这一轮增加多搜索引擎切换配置
